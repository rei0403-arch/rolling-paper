plugins {
    id "com.android.application"
}

android {
    namespace "com.codexplayground.rollingpaper"
    compileSdk 36
    compileSdkExtension 1

    defaultConfig {
        applicationId "com.codexplayground.rollingpaper"
        minSdk 26
        targetSdk 36
        versionCode 1
        versionName "1.0"
    }
}
